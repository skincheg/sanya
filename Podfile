# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VideoPreview' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VideoPreview
	pod 'Alamofire'
	pod 'SwiftyJSON'
	pod 'Firebase/Storage'
	pod 'Firebase/Firestore'
  target 'VideoPreviewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VideoPreviewUITests' do
    # Pods for testing
  end

end
